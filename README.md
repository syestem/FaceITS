# 🎮 FaceITS — Match Flow Plugin для CS2 (CounterStrikeSharp)

FaceITS — плагин для CounterStrike 2, который автоматизирует матчи в стиле FACEIT:

✅ готовность капитанов

✅ ножевой раунд

✅ выбор стороны

✅ стадийные сообщения

✅ стадийные серверные команды

✅ таблица урона после каждого раунда

✅ полностью кастомный флоу через JSON

Все тайминги, тексты и команды настраиваются в одном конфиге.

---

## 📦 Требования

• CS2 Dedicated Server

• CounterStrikeSharp

• .NET 7+

---

## 🚀 Установка

1️⃣ Скопируйте `FaceITS.dll` в:

```
csgo/addons/counterstrikesharp/plugins/FaceITS/
```

2️⃣ Перезапустите сервер или выполните:

```
css_plugins reload FaceITS
```

3️⃣ При первом запуске автоматически создастся конфиг:

```
csgo/addons/counterstrikesharp/configs/plugins/FaceITS/FaceITS.json
```

После этого можно сразу редактировать JSON под свои нужды.

---

## 👑 Капитаны

В конфиге указываются SteamID64 двух капитанов:

```json
"CaptainSteamIds": [
  "7656119...",
  "7656119..."
]
```

Только капитаны могут:

• подтверждать готовность

• выбирать сторону после ножевого

---

## ✅ Готовность

Капитаны пишут в чат:

```
!r
!ready
```

Дальше плагин автоматически переключает стадии:

• первый капитан → `Ready1`

• второй капитан → `BothReady`

Что происходит дальше — полностью задаётся через конфиг.

---

## 🔪 Ножевой раунд и выбор стороны

После стадии `BothReady` обычно запускается ножевой (через StageCommands).

Когда ножевой окончен:

• определяется победившая команда

• определяется капитан победителей

• запускается стадия `KnifeChoice`

Капитан может написать:

```
!stay
!switch
```

Также поддерживаются:

```
!t
!ct
```

Логика умная:

👉 если капитан сейчас за T

• `!t` = остаться

• `!ct` = сменить


👉 если капитан сейчас за CT

• `!ct` = остаться

• `!t` = сменить

После выбора автоматически идут стадии:

• `KnifeChoiceStay`
• или `KnifeChoiceSwitch`
→ затем `FirstRound`

---

## 🧭 Стадии матча (Stages)

Каждый этап матча — это стадия.

Доступные стадии:

```
Warmup
Ready1
BothReady
KnifeRound
KnifeChoice
KnifeChoiceStay
KnifeChoiceSwitch
FirstRound
EveryRoundStart
EveryRoundEnd
```

Ты можешь вешать **любые сообщения и команды** на любую стадию.

---

## 💬 StageMessages — сообщения

Пример:

```json
{
  "Stage": "Warmup",
  "Text": "Привет!\nНапишите !r",
  "Delay": 0,
  "Interval": 25
}
```

### Поля:

• Stage — стадия

• Text — текст (multiline через `\n`)

• Delay — задержка перед первым выводом (секунды)

• Interval — интервал повтора

### Правила:

🟢 `Interval = -1` → выводится один раз

🟢 `Interval >= 0` → повторяется каждые N секунд

🟢 `Text = ""` → сообщение отключено

Можно добавлять сколько угодно сообщений на любую стадию.

---

## 🛠 StageCommands — серверные команды

Работают так же, как сообщения, но выполняют команды сервера:

```json
{
  "Stage": "BothReady",
  "Command": "mp_warmup_end",
  "Delay": 16,
  "Interval": -1
}
```

Используется для:

⚙️ запуска ножевого

⚙️ завершения разминки

⚙️ паузы

⚙️ exec файлов

⚙️ любых server commands

---

## 🔤 Переменные

Можно использовать в Text и Command.

### Команды и счёт

```
{CT}        — название CT команды
{T}         — название T команды
{CTSCORE}   — счёт CT
{TSCORE}    — счёт T
```

---

### Ножевой раунд

```
{KNIFE_CHOICE_TEAM_WINNER}
{KNIFE_CHOICE_CAPTAIN_WINNER}
```

Глобальные на весь матч.

---

### Таблица урона (только в DamageRow)

```
{NAME}        — ник врага
{DEALT}       — сколько ТЫ нанёс ЭТОМУ врагу
{TAKEN}       — сколько ЭТОТ враг нанёс ТЕБЕ
{DEALTHITS}   — твои хиты по нему
{TAKENHITS}   — его хиты по тебе
{HP}          — текущее HP этого врага
```

---

## 📊 Таблица урона

После каждого раунда (`EveryRoundEnd`) выводится:

• `DamageHeader`— общий заголовок

• `DamageRow` — строки по каждому противнику

Важно:

⭐ таблица персональная

⭐ каждый игрок видит свой урон по каждому врагу

⭐ HP показывается для конкретного противника

Это не глобальная статистика — всё индивидуально.

---

## ⏸ Техническая пауза

Команда:

```
!tech
```

Выполняет:

```
mp_pause_match
```

---

## 🔄 Перезагрузка плагина

```
css_plugins reload FaceITS
```

Полностью сбрасывает состояние матча.

---

## 🐞 Отладка

Все переходы стадий пишутся в консоль сервера:

```
[FaceITS] STAGE → Warmup
[FaceITS] STAGE → BothReady
[FaceITS] STAGE → KnifeChoice
...
```

Очень помогает при настройке.

---

## 🔁 Типичный flow матча

Warmup

⬇

Ready1

⬇

BothReady

⬇

KnifeRound

⬇

KnifeChoice

⬇

KnifeChoiceStay / KnifeChoiceSwitch

⬇

FirstRound

⬇

EveryRoundStart / EveryRoundEnd

---

## ❤️ Итог

FaceITS — это:

✨ гибкая стадийная система

✨ полный контроль через JSON

✨ индивидуальная таблица урона

✨ FACEIT-подобный матч-флоу

✨ минимум хардкода

Плагин специально сделан под кастомки и турниры.
