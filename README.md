# ⚙️ Пример FACEIT-конфигов (5 CFG файлов)

В комплекте с FaceITS идут **5 примерных CFG**, которые повторяют стандартный FACEIT-workflow:

```
gamemode_competitive.cfg
start_knife.cfg
knife_stay.cfg
knife_switch.cfg
start.cfg
```

Эти файлы — **не обязательны**, они идут как **референс**.

Ты можешь:

✅ использовать их как есть

✅ изменить под себя

✅ полностью заменить своими StageCommands

FaceITS не зависит от конкретных cfg — всё вызывается через StageCommands.

---

# 🧠 Общая логика

Эти конфиги реализуют классический FACEIT флоу:

```
Warmup
↓
BothReady
↓
Knife round
↓
Knife win
↓
Stay / Switch
↓
Live match
```

FaceITS управляет стадиями,

CFG управляют серверными настройками.

---

# 📁 gamemode_competitive.cfg — базовый режим матча

Это основной конфиг сервера.

Используется для:

🟢 competitive rules

🟢 overtime

🟢 money system

🟢 warmup

🟢 team names

🟢 TV / demo

🟢 freeze time

🟢 buy rules

Ключевые моменты:

### Команды команд

```cfg
mp_teamname_1 TEAM_1
mp_teamname_2 TEAM_2
```

Используются в FaceITS как:

```
{CT}
{T}
```

---

### Бесконечная разминка

```cfg
mp_warmuptime 99999999999
mp_warmuptime_all_players_connected 99999999999
```

Разминка идёт вечно, пока FaceITS не вызовет:

```
mp_warmup_end
```

(через StageCommands)

Это полностью повторяет FACEIT.

---

### Овертаймы

```cfg
mp_overtime_enable 1
mp_overtime_maxrounds 6
mp_overtime_startmoney 10000
```

---

# 🔪 start_knife.cfg — запуск ножевого раунда

Вызывается на стадии `BothReady`.

Основная задача:

• убрать оружие

• включить фризтайм

• оставить только ножи

Ключевые строки:

```cfg
mp_startmoney 0
mp_free_armor 1
mp_ct_default_secondary ""
mp_t_default_secondary ""
mp_give_player_c4 0
```

Также начинает запись demo:

```cfg
tv_record alast_demo_01
```

---

# 🧭 knife_stay.cfg — капитан остаётся на стороне

Если победитель ножевого выбирает **остаться**, выполняется:

```
exec knife_stay
```

Что делает:

✅ включает friendlyfire

✅ возвращает стартовые деньги

✅ включает overtime

✅ рестартит матч

```cfg
mp_restartgame 1
mp_unpause_match
```

После этого начинается LIVE.

---

# 🔄 knife_switch.cfg — капитан меняет сторону

Если капитан выбирает смену сторон:

```cfg
mp_swapteams
exec knife_stay
```

Сначала меняет команды местами,

потом применяет те же настройки что и `knife_stay`.

---

# ▶️ start.cfg — прямой старт без ножевого (опционально)

Используется если ты хочешь **пропускать knife**.

Сразу запускает полноценный матч:

• деньги

• overtime

• рестарт

Также стартует demo:

```cfg
tv_record blast_demo_01
```

---

# 🔗 Как это связывается с FaceITS

FaceITS вызывает CFG через `StageCommands`.

Пример:

```json
{
  "Stage": "BothReady",
  "Command": "exec start_knife",
  "Delay": 15,
  "Interval": -1
}
```

```json
{
  "Stage": "KnifeChoiceStay",
  "Command": "exec knife_stay",
  "Delay": 1,
  "Interval": -1
}
```

```json
{
  "Stage": "KnifeChoiceSwitch",
  "Command": "exec knife_switch",
  "Delay": 1,
  "Interval": -1
}
```

То есть:

🧠 FaceITS = логика матча

⚙️ CFG = настройки сервера

---

# ✅ Почему так сделано

Такой подход даёт:

✨ полный контроль

✨ легко менять правила

✨ можно подключать свои cfg

✨ FACEIT-поведение без хардкода

✨ разные лиги — разные конфиги

---

# 🏁 Коротко

Эти 5 файлов:

• повторяют FACEIT flow

• показывают как работать со StageCommands

• служат примером

• не являются обязательными

FaceITS может работать вообще без них — всё решает JSON.
